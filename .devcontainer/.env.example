# =============================================================================
# POS Restaurant System — Environment Variables
# Copy this file to .env and fill in the values before starting the DevContainer
# =============================================================================

# ---------------------------------------------------------------------------
# Database (PostgreSQL)
# ---------------------------------------------------------------------------
POSTGRES_DB=pos_db
POSTGRES_USER=pos_user
POSTGRES_PASSWORD=pos_password

# ---------------------------------------------------------------------------
# Backend (Quarkus)
# ---------------------------------------------------------------------------
QUARKUS_DATASOURCE_JDBC_URL=jdbc:postgresql://db:5432/pos_db
QUARKUS_DATASOURCE_USERNAME=pos_user
QUARKUS_DATASOURCE_PASSWORD=pos_password
QUARKUS_LOG_LEVEL=ERROR

# ---------------------------------------------------------------------------
# Frontend (Angular)
# ---------------------------------------------------------------------------
NODE_ENV=development
API_URL=http://backend:8080
WS_URL=ws://backend:8080/ws

# ---------------------------------------------------------------------------
# JWT (used if JWT auth is implemented instead of Basic Auth)
# ---------------------------------------------------------------------------
JWT_SECRET=change-me-in-production-use-a-long-random-string
JWT_EXPIRATION_HOURS=8
